/vendor
/node_modules
composer.lock
.phpunit.result.cache
.DS_Store
/.phpunit.cache
.playwright-mcp

# The build brief and conventions are working documents, kept out of the public repo.
/docs/private

# Statamic's marketplace review skill, kept local: it is their file, not ours.
/.agents
